# 作業履歴

## 2026-04-13
- リモートリポジトリ（origin: https://github.com/razilyis/zmk-config-roBa）の状態に合わせて全ブランチを同期。
- `Dev` ブランチを新規にローカルで追跡開始。
- `main` および `cirque-touch_pad` ブランチを最新状態にプル。
- リモートで削除済みの `IQE7211E-touch_pad` ブランチをローカルからも削除しました。
- 作業ブランチを `Dev` に切り替えました。
- スクロールレイヤーを 5 (`Setting_Layer`) から 3 (`Win-Num_Layer`) に変更しました。
- `arrows`（トラックボールによる方向キー入力）機能を有効化し、レイヤー 6 に割り当てました。
- レイヤー 0 の `I` キーを長押しでレイヤー 6 が有効になるよう変更しました（`&lt 6 I`）。
- レイヤー切り替え（`&lt`）のレスポンス向上を試みましたが、誤爆等により使いにくくなったため、設定を削除して ZMK の標準設定に戻しました。
